#include<bits/stdc++.h>
using namespace std;

int main(){
    int tc;
    cin >> tc;
    while(tc--){
        int w,t;
        cin >> w >>t;
        
        vector<int> v(t);
        for(int i=0;i<v.size();i++){
            cin  >> v[i];
        }

        

    }
}